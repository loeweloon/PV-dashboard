# env files (contain secrets — never commit)
.env.local
.env*.local

# dependencies
node_modules/

# Next.js build output
.next/

# extracted zip contents (source already committed to src/)
pv-dashboard/
